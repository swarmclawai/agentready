{
  "name": "agentready-monorepo",
  "private": true,
  "version": "0.1.0-alpha.0",
  "description": "Open-source readiness scanner for the agentic web.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "dev": "pnpm --filter @swarmclawai/agentready dev",
    "lint": "pnpm typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b packages/types packages/core packages/rules packages/report packages/cli --pretty false",
    "pack:cli": "pnpm --filter @swarmclawai/agentready pack",
    "release:check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
