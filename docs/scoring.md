# Scoring

AgentReady scores from `0` to `100` using profile-specific category weights.

Status values:

- `pass`: full credit
- `warn`: half credit
- `unknown`: quarter credit
- `fail`: no credit
- `info` and `not_applicable`: excluded from score

The score is a readiness signal, not a certification. Reports should explain uncertainty rather than overclaim precision.
