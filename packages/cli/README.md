# AgentReady

Open-source readiness scanner for the agentic web.

```bash
npx @swarmclawai/agentready scan https://example.com --profile merchant
```

The package name is `@swarmclawai/agentready`; the executable is `agentready`.
